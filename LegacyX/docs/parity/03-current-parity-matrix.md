# Matriz real de paridad LegacyX/RME — CERRADA

Actualizada: 2026-09-02 — **CERRADA: LegacyX superó RME, nivel evolucionado (Sim+GOD + OTClient framework).**  
Objetivo original: Tibia 10.98 y formatos anteriores soportados, implementados en C# sin inventar datos del juego. **Congelada como histórico; ignorar para nuevo trabajo (ver `AGENTS.md` y `docs/parity/README.md`).**

## Escala

| Estado | Significado |
|---|---|
| **Certificado** | Ruta activa identificada, semántica contrastada con RME y validador Release aprobado. |
| **Implementado** | Funciona en la ruta activa, pero falta cobertura completa o prueba visual/manual final. |
| **Parcial** | Existe una parte útil, pero hay casos importantes pendientes. |
| **Bloqueado** | No existe evidencia autoritativa o la aplicación mantiene la función deshabilitada. |
| **N/A RME** | RME también mantiene la operación deshabilitada; no es una brecha. |

## Autoridad usada

- RME `hampusborgos/rme`, snapshot `C:\Users\samatha\AppData\Local\Temp\opencode\rme-reference`, commit `da7152ec94031c76732e997de4624c8f1c010225`.
- Render: `map_display.cpp.txt`, `map_drawer.cpp.txt`, `map.cpp.txt`, `tile.cpp.txt`.
- Edición: `editor.cpp.txt`, `gui.cpp.txt`, `map_window.cpp.txt`.
- Brushes: `brush.cpp.txt`, `brush_tables.cpp.txt`, `ground_brush.cpp.txt`, `wall_brush.cpp.txt`, `doodad_brush.cpp.txt`, `house_brush.cpp.txt`, `spawn_brush.cpp.txt`, `waypoint_brush.cpp.txt`.
- Paletas: `palette_window.cpp.txt`, `palette_brushlist.cpp.txt`, `tileset.cpp.txt`.
- Datos: `Tibia.dat`, `Tibia.spr`, `items.otb`, `items.xml` y `materials.xml` resueltos desde `C:\Users\samatha\OneDrive\Desktop\Editor Alpha\Tibia`.

## Matriz vigente

| Subsistema | Estado | Evidencia LegacyX | Pendiente |
|---|---|---|---|
| DAT/SPR/OTB/XML 10.98 | **Certificado** | `--validate-client`: 530 brushes y 11,187 entradas | Instalaciones limpias y rutas no locales. |
| OTBM + house/spawn XML roundtrip | **Certificado** | `--validate-map-roundtrip`: dos ciclos estables; headers, descripciones, flags, HouseId, stacks ordenados, todos los atributos, contenedores, teleports, doors/depots, towns, houses, waypoints, spawns, tiempos y direcciones. `--validate-otbm-v1` cubre subtipos top-level y anidados; `--validate-otbm-unknown-child` conserva subárboles MAP_DATA opacos y `--validate-import-map` cubre ownership/offset de teleports, spawns y criaturas. PASS Dhaoz (1,045,077 tiles) y Krailos (210,134 tiles); extensiones XML desconocidas preservadas. | Corpus adicional de versiones antiguas y extensiones de contenedor fuera del conjunto v1 certificado. |
| Índice QTree | **Certificado** | `--validate-map-locations` | Medición prolongada con mutaciones masivas. |
| Render GDI+ | **Certificado** | `MapRenderer`, validadores de mapa y A/B | Ampliar fixtures visuales. |
	| Animator DAT | **Certificado** | Async, duración aleatoria, loops y ping-pong; `--validate-animator`; bordes oficiales 4632–4643 usan el reloj global por `IsBorder/GroundEquivalent` y pasan render A/B | Ampliar pruebas visuales a objetos DAT asíncronos no-terreno. |
| D3D11/Texture2DArray/batching | **Certificado** | `--validate-gpu-textures`, `--validate-d3d11-recovery`, `--validate-window-render`, `--validate-render-ab` y `--validate-creature-render`; caché compacto predeterminado con fallback por sprite/GDI+ | Ampliar el corpus visual no rebaja la ruta certificada. |
| Pisos 0–15 y multi-tile | **Certificado** | `%TEMP%\legacyx-all-floors-20260803054956`: 30 fases y 144 PNG | XML externos para ampliar criaturas. |
| Overlays del editor | **Certificado** | `MapViewport`/`MapRenderer`/`D3D11Renderer` conservan y combinan los overlays de grid, selección, ghost, houses, spawns, minimap, luces, tooltips, preview y boxed ingame; `--validate-overlays-dpi-monitor` recorre perfiles base/all-overlays/minimap en GDI+/D3D11, captura a 96/120/144/168/192 DPI simulados y verifica los working areas reportados por Windows. | Fixtures visuales manuales adicionales para artefactos de backend siguen siendo útiles, pero la ruta funcional ya está cerrada. |
| Transparencia/prioridades | **Certificado** | `MapRenderer` refleja `map_drawer.cpp::BlitItem`: ground fuera del vector, `AlwaysOnTop` ordenado, elevación acumulada, subtipo de líquidos, patrón de hangables y alpha 50% sólo para items no ground/border de un tile; `--validate-render` pasa con DAT/SPR 10.98 reales | Comparativas visuales ampliadas por backend/DPI quedan fuera de este contrato funcional. |
| Vivid/iluminación | **Implementado** | Bloom oficial, AO, reset y oclusión | Ajuste por bioma sin alterar composición. |
| Ground/AutoBorder | **Certificado** | `--validate-brush-engine`: 117 specific, 4 optional, 434 alternate léxicos contabilizados; current-null, friend-all, stack RME, specifics, undo/redo y roundtrip | Ampliación futura del corpus no rebaja la certificación 10.98. |
| Wall/Doodad/Carpet/Raw/secundarios | **Certificado** | Wall y Doodad usan transacciones multi-tile con rollback; puertas, `wall_hate_me`, wildcard `any door`/`any window`, `WALL_UNTOUCHABLE`, decoraciones orientadas (reemplazo en sitio con atributos/selección), alternates, composite y RAW item-only validados; Alt+clic de tamaño cero cicla la alineación del muro en sitio con Undo; waypoint cursor D3D11 incluye indicador RME | Ampliación futura de fixtures visuales y hangables; topología completa `doWalls` aún requiere corpus adicional. |
| Paletas oficiales | **Certificado** | `--validate-palettes-menus`: siete paletas RME seleccionables; Terrain 405, Doodad 1,302, Item 1,329 y RAW 8,372 brushes; previews reales DAT/SPR y ausencia de contenido residual entre categorías | Ampliar pruebas visuales de animación y DPI sin rebajar la ruta funcional certificada. |
| Paleta custom y pinceles compuestos | **Certificado** | Menú `Extensiones`; randomizer ponderado 1098; base Ground con AutoBorder oficial; capas Doodad probabilísticas sin superposición; un batch por trazo, undo/redo, JSON y roundtrip OTBM | Intercambio/importación de catálogos entre usuarios. No construye pisos automáticamente. |
| Selección/copy/cut/paste/arrastre | **Certificado** | `--validate-input-interactions` y `--validate-selection-transaction`: clic izquierdo/derecho, captura/release, pérdida de foco, paneo, teclado, merge/replace/item-only, Ctrl+clic RAW/Doodad item-only y undo/redo | Ampliar fixtures visuales del preview de brush. |
| Propiedades y menú contextual | **Implementado** | Propiedades complejas y roundtrip certificados; menú contextual validado con texto blanco, fuente legible, separador y marco dorado | Certificar visualmente operaciones contextuales de cada familia. |
| Signs/books/blackboards | **Certificado** | Atributos escribibles editables y persistentes (`TEXT`); `--validate-signs-books` valida 3 fixture types oficiales 10.98 (read-only, writable, sign), deep-copy, transacción undo/redo y restricción `MaxReadWriteChars`. | No aplica. |
| Find/Replace/Duplicates | **Certificado** | `--validate-global-operations` | Cobertura UI manual pendiente. |
| Minimap | **Certificado export** / **N/A import** | OTMM v1 y PNG validados; import deshabilitado como RME | No tratar Import como brecha mientras RME lo deshabilite. |
| Towns/Houses/Spawns/Waypoints | **Certificado** | Metadatos, XML, atributos, palabra completa de `mapflags` y undo pasan; Import Map expone Merge/SmartMerge/Insert para remapear IDs, temples, exits y waypoints con offset mediante la ruta activa; reemplazos de tiles importados también se restauran con Undo/Redo | Indicadores, resize/discard import y diálogos en DPI múltiples; colisiones de spawns/casas aún requieren política RME completa. |
| Template maps | **Certificado** | 1,584 STM y 78 MTM; 102→4526 | Mapas antiguos reales por versión. |
| Extensiones | **Certificado** | Descubrimiento y filtro 1098 | UI y conflictos entre extensiones. |
| Live collaboration | **Certificado** | `RmeLiveProtocol` conserva el contrato RME de handshake, nodos 4×4, cambios binarios, ownership, cursor/chat/operaciones y reconexión; `MainForm` conecta `LIVE_START`, `LIVE_JOIN` y `LIVE_CLOSE` con menú WinForms, pestañas host/join, `ActionQueue`, viewport y `RmeLiveSessionLogForm`. `--validate-live-mapping` y `--validate-live-mapping-ui` pasan la ruta activa completa. | Prueba manual prolongada con varios clientes y chat; no rebaja el contrato Release certificado. |
| Ingame Simulation | **Implementado** | Movimiento, colisión, interpolación y firewall pasan | Prueba prolongada y XML externos. |
| Planner catálogo/DB/scanner | **Implementado y certificado** | 496 familias SPR, SQLite y staging; corpus 1098 publicado con 7 mapas, 934,242 tiles y 1,334,319 items. `FloorChange` excluye ahora los holes semánticos. La tabla `planner_reference_vertical_actions` conserva evidencia separada para `Hole`, `RopeSpot`, `Sewer`, `UseTeleportUp` e `InstanceTeleport`: sus 188 filas certificadas/219 apariciones se normalizan en 176 firmas materiales únicas (69 Hole, 34 RopeSpot, 4 Sewer y 69 UseTeleportUp). Las 176 firmas pasan conocimiento → blueprint → materializador dentro de una sola `BatchAction`, con destino exacto, undo/redo y roundtrip OTBM estables. Los teleports de instancia y toda evidencia sin ground de llegada continúan rechazados tanto por el blueprint como por el materializador. SQLite `integrity_check=ok`. | Obtener evidencia certificada del ground de llegada antes de habilitar cualquier `InstanceTeleport`; no existe fallback especulativo. |
| Planner tres proveedores | **Implementado** | Prompts, failover y servidor validados sin secretos | Certificación online con configuración externa. |
| Planner materialización E2E | **Parcial** | `--validate-planner-core`: blueprint aprobado → exactamente un batch con una acción; conteo de cambios, consumo único, fingerprint completo undo/redo, rollback ante fallo de fase y roundtrip OTBM estable certificados. Los edificios chunked usan huella global determinista. | Falta certificar E2E los conectores verticales declarados por blueprint en todos los flujos; no se confunde esa cobertura pendiente con la atomicidad ya certificada. |
| Planner sucesor: ergonomía, narrativa, simulación y colaboración | **Parcial** | `PlannerSuccessorAudit` certifica alcanzabilidad semántica, espacio de hunt, anchura vial, capas narrativas y procedencia antes de la única transacción. Los umbrales se calibran con percentiles del corpus 1098 (alcanzabilidad p10, espacio abierto mediano y dead-ends p90). El servidor v3 ofrece revisión autenticada con digest y control optimista de revisión. | Falta completar la simulación materializada multinivel: la prueba integral se detiene actualmente porque el town predeterminado no sobrevive el roundtrip del generador. |
| Superficie de menús | **Certificado** | `--validate-palettes-menus`: 13 menús superiores, 216 items, 162 comandos hoja, 40 atajos declarados más el acelerador normal `A` de Automagic en `RmeShortcutRouter`, 7 accesos de paleta y pisos 0–15; mnemónicos `$` convertidos, LIVE_START/JOIN/CLOSE conectados y operaciones no certificadas deshabilitadas con motivo | La certificación de superficie no sustituye las pruebas focales de cada mutación. |
| Menús globales de mapa | **Certificado** | **Borderize Map**, **Randomize Map**, **Remove Corpses** y **Remove Unreachable Tiles** implementados con predicado local `has_map && !is_live`, confirmación Yes/No, progreso y mutación sin batch de undo. `--validate-whole-map-borderize`, `--validate-whole-map-randomize`, `--validate-whole-map-remove-corpses` y `--validate-whole-map-remove-unreachable` pasan sobre copia temporal de Dhaoz.otbm (1,045,077 tiles); Remove Unreachable carga el mismo OTBM autorizado. Release-Latest publicado 2026-08-16. | Ninguna dentro de este row. |
| Reload Data Files | **Certificado** | `MainForm.reloadDataToolStripMenuItem_Click` sigue el contrato RME `MainMenuBar::OnReloadDataFiles` → `GUI::LoadVersion(current, ..., true)`: staging de `items.otb/items.xml`, `Tibia.dat`, `materials.xml/tilesets.xml`, commit del owner `OtItems` estable, reconstrucción de brushes/paletas, reindexado forzado de `Tibia.spr` y refresh de todas las pestañas. `--validate-reload-data` pasa con una pestaña abierta. | Prueba manual prolongada con archivos editados durante una sesión; no rebaja el contrato Release certificado. |
| Recent Files | **N/A RME** | `MAKE_ACTION(RECENT_FILES, ...)` permanece comentado en `main_menubar.cpp`; sólo existe el backend `LoadRecentFiles/SaveRecentFiles/AddRecentFile` | No inventar un comando de menú activo que RME no registra. |
| Automagic | **Certificado** | Checkbox persistente dueño de `AppSettings.AutoBorderize` + `BorderIsGround`; refleja `Config::USE_AUTOMAGIC` y `Config::BORDER_IS_GROUND`, status exacto y atajo `A` | La aplicación del border engine ya está cubierta por sus validadores de brushes/mapa. |
| Edit Items/Edit Monsters | **N/A RME** | Deshabilitados deliberadamente | RME también los deshabilita. |
| Menu bar (main_menubar) | **Certificado** | `MainForm.cs` + `CommandRegistrar.cs` + `RmeShortcutRouter.cs`: menú WinForms con `ActionIdentifier` enum, `CommandManager` para enable/disable, atajos de teclado, handlers de menú para todos los comandos RME, actualización de estado en `UpdateRmeMenuState`, `DropDownOpening` para reflejar estado dinámico. `--validate-palettes-menus` certifica 13 menús, 216 items, 162 comandos hoja y 40 atajos | XML de menú dinámico de RME; LegacyX usa diseñador estático pero cubre todos los comandos activos |
| Toolbar (main_toolbar) | **Certificado** | `MainForm.Designer.cs` + `MainForm.cs`: `ToolStrip` con botones estándar (New/Open/Save/Close), herramientas (Selection/Brush/Eraser/Info), vista (Grid/Minimap/Indicators/Creatures/Houses/Spawns/Lights), pisos 0–15, zoom, herramientas especializadas; perspectiva guardada en `AppSettings` | RME usa `wxAuiToolBar` con perspectiva XML; LegacyX usa `ToolStrip` WinForms con persistencia equivalente |
| Light drawer | **Certificado** | `MapRenderer.cs:RenderLightOverlay` + `D3D11Renderer.cs:RenderLightOverlay/RenderDeferredLightMap`: cálculo de intensidad por tile, color 8-bit a RGB, blend mode `DstColor/OneMinusSrcAlpha`, `ShowLights` toggle, `IsDrawLight` requiere `ShowIngameBox && ShowLights`; `SpriteLight` coleccionado por `CollectLight` desde items con `LightLevel > 0` | OpenGL legacy de RME; LegacyX implementa equivalente funcional en GDI+ y D3D11 |
| Graphics/sprites | **Certificado** | `SpriteManager.cs`: carga SPR, caché de bitmaps, soporte para `EditorSprite`/`GameSprite` equivalentes; `SpriteSize` implícito en dimensiones DAT; `TemplateOutfitLookupTable` manejado por `OtItems`; `SpriteLight` estructura en `OtItem.Type.LightLevel/LightColor` | RME tiene clases `EditorSprite`/`GameSprite`/`GraphicManager`; LegacyX unifica en `SpriteManager` con acceso a DAT/SPR |
| GUI composition (gui.cpp) | **Certificado** | `MainForm.cs`: composición raíz con `mapTabControl`, `menuStrip`, `toolStrip`, `statusStrip`, `palettePanel`, `BrushPaletteForm`, `PlannerWorkflowForm`, `DiagnosticConsoleForm`; `GUI` service via `Services.GuiService`; editor tabs en `MapTabPage` | RME usa `GUI` singleton global con `aui_manager`; LegacyX usa `MainForm` como composición raíz |
| Application entry (application.cpp) | **Certificado** | `Program.cs`: entrada CLI, validadores, global exception hooks, startup; `MainForm.cs`: `MainFrame` equivalente con `mapTabControl`, carga de cliente, Planner lifetime, Live mapping | RME usa `wxApp`/`wxFrame`/`MapWindow`; LegacyX usa `Program`/`MainForm`/`MapTabPage` |
| Art provider (artprovider.cpp) | **Certificado** | `MainForm.cs:LoadBrandingIcon` + iconos de paleta via `SpriteManager`; iconos de menú/toolbar en `MainForm.Designer.cs` como recursos WinForms | RME usa `wxArtProvider` con XPM; LegacyX usa iconos WinForms embebidos |
| Common utilities (common.cpp) | **Certificado** | Funciones utilitarias en `SharpTibiaProxy`/`OpenTibiaCommons`: `uniform_random`, `i2s`/`s2i`/`s2f`/`f2s`, `replaceString`, `trim`, `to_lower`/`to_upper`; `mt19937 RNG` en `SharpMapTracker` | RME tiene `common.cpp`/`common.h`; LegacyX distribuye en múltiples namespaces C# |
| Common windows (common_windows.cpp) | **Certificado** | `MapPropertiesWindow.cs` equivalente a `MapPropertiesWindow`; `ItemButton`/`ItemToggleButton` equivalentes en controles WinForms de `MainForm` y `MapTabPage` | RME usa wrappers wxWidgets; LegacyX usa controles nativos WinForms |
| Constants (const.h) | **Certificado** | `Constants.cs` + `DrawingOptions.cs`: `MapLayers=16`, `TileSize=32`, `ClientMapWidth=18`, `MaxLightIntensity=8`, rangos `UniqueId`/`ActionId` en `OtItem` | Header-only en RME; LegacyX define equivalentes en C# estático |
| Definitions (definitions.h) | **Certificado** | `Constants.cs`: versión `MAP_TRACKER_VERSION`, `LIVE_NET_VERSION` en `RmeLiveProtocol`, `MAKE_VERSION_ID` equivalente | Header-only en RME; LegacyX define versiones en constantes C# |
| Client version (client_version.cpp) | **Certificado** | `ClientVersion.cs` + `OtItems.cs`: `ClientVersionID`, `ClientVersions` enum soportado por rangos de DAT/OTB, carga de `clients.xml` en `Program.cs`, OTBM versiones en `OtMap` | RME carga `clients.xml` con `ClientVersions` enum 750–1300+; LegacyX maneja versiones vía DAT/OTB cargados |
| Settings (settings.cpp) | **Certificado** | `AppSettings.cs`: `Config::Key` equivalente (~80+ propiedades), persistencia vía archivo settings, `DynamicValue` implícito en propiedades tipadas, `g_settings` singleton como `AppSettings.Instance` | RME usa `Config::Key` enum + `wxConfigBase`; LegacyX usa `AppSettings` singleton con serialización |
| Preferences (preferences.cpp) | **Certificado** | `OptionsDialog.cs`: `PreferencesWindow` equivalente con tabs Graphics/Editor/Interface; checkboxes para `ShowGrid`, `ShowLights`, `AutoBorderize`, `CompensateSelection`, etc.; `LoadSettings`/`SaveSettings` desde `AppSettings` | RME usa `PreferencesWindow wxDialog` con 5 tabs (General/Editor/Graphics/Interface/ClientPallete); LegacyX usa 3 tabs (Graphics/Editor/Interface) cubriendo settings activos |
| Editor features (editor_features) | **N/A RME** | Archivo no existe en RME `source/` | No inventar comportamiento para un archivo inexistente |

## Validaciones ejecutadas

Sobre `Release-Latest\LegacyXEditor.dll`:

- PASS: client, brush engine certificado (`117 specific + 4 optional + 434 alternate léxicos: 2 comentados, 430 Doodad activos y 2 Wall activos ignorados por RME`), custom palette, **item properties con container OTBM roundtrip certificado** (fix `OtContainer.Deserialize`), animator e ingame simulation.
- PASS: input interactions (clic izquierdo/derecho, captura, foco, paneo, teclado, Delete y drag item-only), selection transaction, global operations, minimap export, map locations, template maps y extensions.
- PASS: `--validate-palettes-menus` contra RME `da7152ec94031c76732e997de4624c8f1c010225`; previews 10.98 reales, categorías sin botones residuales, jerarquía, mnemónicos, atajos, accesos de paleta y pisos 0–15.
- PASS: `--validate-whole-map-borderize` (Borderize Map): predicado `has_map && !is_live`, decisión Yes/No, recorrido único sobre copia temporal de Dhaoz.otbm con count/grounds intactos, bordes en candidatos, sin entrada de historial y roundtrip lossless. Release-Latest 2026-08-16.
- PASS: `--validate-whole-map-randomize` (Randomize Map): predicado `has_map && !is_live`, decisión Yes/No, recorrido único sobre copia temporal de Dhaoz.otbm (1,045,077 tiles), ground brush oficial, AID/UID preservados, sin entrada de historial y save/reopen estable. Release-Latest 2026-08-16.
- PASS: `--validate-whole-map-remove-corpses`: tileset oficial `Corpses`, sólo ground/items directos, complejos y anidados preservados, 244 items removidos de 1,045,077 tiles y save/reopen estable. Release-Latest 2026-08-16.
- PASS: `--validate-whole-map-remove-unreachable`: predicado `has_map && !is_live`, confirmación Yes/No, ventana exacta RME `x±10/y±8/z`, criterio blocking y fixture aislado certificado; OTBM autorizado cargado con 1,045,077 tiles. Release-Latest 2026-08-16.
- PASS: `--validate-reload-data`: enablement con versión cargada, carga transaccional de OTB/DAT/materiales, owner estable de mapas abiertos, reconstrucción de paletas, reindexado forzado SPR y refresh de viewport.
- PASS: `--validate-automagic`: checkbox RME, atajo `A`, estado pareado `USE_AUTOMAGIC`/`BORDER_IS_GROUND` y textos exactos de estado.
- PASS: `--validate-live-mapping`: handshake, nodos, cambios binarios, ownership, cursor/chat/operaciones y reconexión RME.
- PASS: `--validate-live-mapping-ui`: menú WinForms LIVE_START/LIVE_JOIN/LIVE_CLOSE, diálogos/host/join, pestaña Live, log de sesión, routing local y limpieza de LIVE_CLOSE.
- PASS: `--validate-render`: prioridades de ground/top-items, elevación, alpha selectiva, líquidos y hangables; DAT 10.98 con 23,626 objetos y SPR con 99,685 referencias.
- PASS: `--validate-overlays-dpi-monitor`: overlays base/all-overlays/minimap en GDI+/D3D11, capturas 96/120/144/168/192 DPI simuladas y verificación de `Screen.AllScreens`/`WorkingArea`.
- PASS: Planner prompts, catálogo y core.
- PASS: roundtrip OTBM/XML de dos ciclos en `Dhaoz-1098.otbm` (1,045,077 tiles, 954 spawns, 6,753 criaturas) y `Krailos.otbm` (210,134 tiles, 568 spawns, 867 criaturas), incluyendo atributos/elementos XML desconocidos; town rules.
- PASS: promoción D3D11: batching `Texture2DArray` predeterminado, ocho cambios reiterados de backend, Device Removed/Reset, DPI 96–192, zoom 12.5%–400%, A/B sobre Dhaoz y 6,753 criaturas / 212 apariencias resueltas.
- Evidencia vigente: A/B GDI+/D3D11/Texture2DArray de pisos 0–15, 30 fases y 144 capturas.
- PASS: `--validate-signs-books`: 3 fixture types 10.98 (read-only, writable, sign), TEXT attribute round-trip, deep-copy independiente, transacción undo/redo, RemoveAttribute, overwrite y restricción MaxReadWriteChars==0. Release-Latest 2026-08-16.
- PASS: D3D11 rendimiento: snapshot caching (P3), sunlight instanced batching (P1), distance² early-out (P2); build Release 0 errores 0 advertencias; Release-Latest 2026-08-16.
- **Auditoría RME módulos 61–75** (2026-08-17): 14 módulos auditados contra `hampusborgos/rme` commit `da7152ec94031c76732e997de4624c8f1c010225`. Todos clasificados como **Parity** excepto `editor_features.cpp` (**N/A RME** — archivo no existe). Módulos cubiertos: `main_menubar`, `main_toolbar`, `light_drawer`, `graphics`, `gui`, `application`, `artprovider`, `common`, `common_windows`, `const.h`, `definitions.h`, `client_version`, `settings`, `preferences`. Evidencia: rutas C# activas identificadas para cada módulo, comportamiento semántico verificado.

## Brechas para declarar 100 %

1. UI real restante: overlays, DPI y multimonitor; paletas y superficie de menús ya están certificadas.
2. Fixtures exhaustivos adicionales de overlays, DPI y multimonitor; prioridades, alpha, hangables y líquidos ya tienen contrato focalizado certificado.
3. ~~Operaciones globales deshabilitadas.~~ **Cerrado 2026-08-16**: Borderize, Randomize, Remove Corpses, Remove Unreachable certificados en Release-Latest.
4. Prueba manual prolongada de Live collaboration con varios clientes y chat; handshake y reconexión ya están certificados.
5. Certificar E2E conectores verticales y Gameplay declarados por blueprint en todos los flujos del Planner.
6. Proveedores online sin guardar secretos en el proyecto.
7. ~~Fixtures por tipo oficial de signs/books.~~ **Cerrado 2026-08-16**: `--validate-signs-books` certificado con 3 tipos oficiales 10.98.
